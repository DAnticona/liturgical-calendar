import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'liturgical-calender',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
